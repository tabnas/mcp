# CI caller. Live: change it here, in a reviewed pull request (session
# credentials push .github/workflows/*; admin DECISIONS.md ADR-8, as
# amended 2026-09-24).
#
# ts-only: this repo is tooling over the engine, not a parity package —
# there is no go/ tree (AGENTS.md "What this project is").
name: ci

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  ci:
    uses: tabnas/.github/.github/workflows/polyglot-ci.yml@main
    with:
      deps: "parser support"
      build-order: "parser support mcp"
      run-go: false
