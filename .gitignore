node_modules
dist

# wrangler local state (dev cache, deploy metadata)
.wrangler

# generated into ts/src by the build (sources of truth: data/ and package.json)
ts/src/data-bundle.ts
ts/src/grammar-validator.js

# Vale's downloaded style packages. The vocabulary under
# .vale/styles/config/ and the house rule under .vale/styles/Tabnas/
# are ours and ARE committed; Google/ arrives from the pinned URL
# in .vale.ini via `vale sync`.
/.vale/styles/Google/
